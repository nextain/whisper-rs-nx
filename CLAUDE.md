# whisper-rs-nx — AI Agent Entry Point

AI-assisted contributions are welcome in this project. See `CLAUDE.md` for full context.

## Quick Start for AI Agents

1. Read `.agents/context/upstream.yaml` — upstream whisper-rs conventions (MUST follow)
2. Read `.agents/context/architecture.yaml` — project structure
3. All code must pass `cargo fmt` and `cargo clippy`
4. Follow upstream code style, not naia-os or other project conventions

## Key Constraint

This is a fork of whisper-rs. All Rust code must look like it belongs in the upstream repository.
See `.agents/context/upstream.yaml` for the complete upstream style reference.
